import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('TaskMaster', () => App);

// For React Native CLI on Android/iOS, the app name is read from native projects.
// This file ensures Metro has a valid entry point.

